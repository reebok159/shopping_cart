%a.dropdown-toggle.lead.small{href: "#", 'data-toggle': "dropdown", role: "button", 'aria-haspopup': "true", 'aria-expanded': "false"}
  = active_order_filter
  %i.fa.fa-angle-down.dropdown-icon.pull-right
%ul.dropdown-menu
  %li
    = link_to t('orders.filter.all'), orders_path
  - order_filters.each do |item|
    %li
      = link_to t("orders.status.#{item[0]}"), orders_path(status: item[1])
